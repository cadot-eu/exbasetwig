version: '3'
services:
  cadot-info-twigbundle:
    image: cadotinfo/symfony5
    container_name: cadot-info-twigbundle
    volumes:
      - .:/app
    networks:
      - web
    restart: always
    ports:
      - 80
 
networks:
  web:
    external: true
